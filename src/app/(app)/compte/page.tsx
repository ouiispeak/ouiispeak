import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Compte — OuiiSpeak' };

export default function ComptePage() {
  return (
    <main>
      <h1>Compte</h1>
      <p>Paramètres du compte utilisateur.</p>
    </main>
  );
}
